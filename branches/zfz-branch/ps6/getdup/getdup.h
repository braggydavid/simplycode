#ifndef INCLUDED_GETDUP
#define INCLUDED_GETDUP

#include <vector>

bool hasdup(const std::vector<int>& v);
int getdup(const std::vector<int>& v);
int getonedup(const std::vector<int>& v);


#endif /* INCLUDED_GETDUP */
